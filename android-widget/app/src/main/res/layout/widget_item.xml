<?xml version="1.0" encoding="utf-8"?>
<!-- Matches web app .todo-card dark style: card-bg=#0d0f1e, left color bar, corner decoration -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:paddingEnd="10dp">

    <!-- Left color bar (3dp, matches web border-left: 3px solid var(--cc)) -->
    <View
        android:id="@+id/widget_item_accent"
        android:layout_width="3dp"
        android:layout_height="match_parent"
        android:minHeight="36dp"
        android:layout_marginEnd="10dp"
        android:background="@drawable/bg_accent_bar" />

    <!-- Content -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Meta row: category badge + time -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="2dp">

            <!-- Category badge (text, colored per category) -->
            <TextView
                android:id="@+id/widget_item_category"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="8sp"
                android:fontFamily="monospace"
                android:letterSpacing="0.05"
                android:textColor="#00ffe7"
                android:paddingHorizontal="4dp"
                android:paddingVertical="1dp" />

            <!-- Time (text3 color) -->
            <TextView
                android:id="@+id/widget_item_time"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="end"
                android:textSize="8sp"
                android:fontFamily="monospace"
                android:textColor="#4a4f72" />

        </LinearLayout>

        <!-- Title (Rajdhani→monospace, text color #e8eaf6) -->
        <TextView
            android:id="@+id/widget_item_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#e8eaf6"
            android:fontFamily="monospace"
            android:singleLine="true"
            android:ellipsize="end" />

    </LinearLayout>

    <!-- Check indicator (matches web app done state: cyan when done) -->
    <TextView
        android:id="@+id/widget_item_check"
        android:layout_width="16dp"
        android:layout_height="16dp"
        android:layout_marginStart="8dp"
        android:gravity="center"
        android:textSize="9sp"
        android:textColor="#00ffe7"
        android:fontFamily="monospace"
        android:background="@drawable/bg_widget_check" />

</LinearLayout>
